#include <stdio.h>

/**
 * main - function run starts here
 * @argc: argument count
 * @argv: argument vector, an array of char
 *
 * Return: Always 0
 */
int main(argc, char *argv[])
{
	if (argc > 0)
		printf("%s\n", argv[0]);


	return (0);
}

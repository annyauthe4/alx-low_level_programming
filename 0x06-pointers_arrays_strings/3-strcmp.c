#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: first string
 * @s2: 2nd string
 *
 * Return: s1 - s2
 */
int _strcmp(char *s1, char *s2)
{
	int i = 0; j = 0, k = 0, len, a;

	while (*s1)
	{
		i++;
	}
	while (*s2)
	{
		j++;
	}
	if (a <= b)
	{
		len = a;
	}
	else

		len =b;
}
